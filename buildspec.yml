version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install
  build:
    commands:
      - npm run generate
  post_build:
    commands:
      - aws s3 cp dist s3://tabelaprovisoria-site --recursive
artifacts:
  file:
    - '**/*'
  base-directory: dist